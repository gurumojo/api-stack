version: '2'
services:
  swagger:
    image: ${SWAGGER_IMAGE}
    ports:
      - "${SWAGGER_PORT}:${SWAGGER_PORT}"
    networks:
      - stack
    depends_on:
      - api
    environment:
      - API_URL
