#!/bin/bash -e

NAME=${1:-openssl}
PATH=${2:-.}
KEY=${PATH}/${NAME}.key
CRT=${PATH}/${NAME}.crt

/usr/bin/openssl req \
	-x509 -nodes -days 3650 \
	-subj "/C=/ST=/L=/O=/CN=$NAME" \
	-newkey rsa:2048 -keyout $KEY -out $CRT

/bin/chmod 600 ${KEY} ${CRT}
